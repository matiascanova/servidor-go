version: "2"
sql:
  - engine: "postgresql"
    schema: "sql/schema/schema.sql"
    queries: "sql/queries/queries.sql"
    gen:
      go:
        package: "db"
        out: "db"
        sql_package: "database/sql"
        emit_json_tags: true
